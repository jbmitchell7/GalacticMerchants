<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GalacticMerchants" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/magenta</item>
        <item name="colorOnPrimary">@color/yellow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/magenta</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black_theme</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/yellow</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="android:windowBackground">@color/black_theme</item>
    </style>
    <style name="Login" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <!-- disables action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- sets colors -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black_theme</item>
        <item name="android:windowBackground">@color/black_theme</item>
        <item name="android:textColorHint">@color/gray</item>
        <item name="android:textColor">@color/yellow</item>
        <item name="android:editTextColor">@color/black</item>
    </style>
    <style name="Main" parent="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <!-- sets colors -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black_theme</item>
        <item name="android:windowBackground">@color/black_theme</item>
        <item name="android:textColor">@color/yellow</item>
    </style>
</resources>